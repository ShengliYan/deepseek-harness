{
  "_comment": "feedUrl 是 app 检查更新的地址。发布构建留空。本地开发循环把 file://…/dist/latest.json 写进同目录的 update-config.local.json（已 gitignore）；zipBaseUrl 仅在需要 https 分发时填写（留空则生成本地相对路径 feed；updater 拒绝 http）。",
  "feedUrl": "",
  "zipBaseUrl": ""
}
